import sliderStyles from "../styles/ImagesSlider.module.css";

const ImagesSlider = ({ images }) => {
  console.log(images);
  return <div className={sliderStyles.container}></div>;
};

export default ImagesSlider;
